# 🗺️ ROADMAP.md — Text Game RPG: все идеи и направления развития

> **Цель:** живая текстовая RPG с ИИ-рассказчиком и бессмертной памятью мира.
> Формат: статусы `✅ Done` / `🟡 Next` (ближайшие) / `🟢 Planned` (среднесрочные) / `🔵 Backlog` (на будущее).
> Обновляй после каждой сессии: переноси сделанное в Done с короткой пометкой.
>
> **Этот файл — единый план работ.** `PROPOSALS.md` (отдельный список «идеи + оценка эффект/сложность»)
> с сессии 33 **слит сюда** и удалён: два списка одних и тех же задач расходились.
> Разметка: `[ПР]` — было в ROADMAP, `[PR]` — пришло из PROPOSALS, `[AN]` — из «Актуального анализа»
> (папка `НЕ УДАЛЯТЬ ПАПКУ. ИДЕИ/`), `[OWN]` — новое.

---

## ⚖️ ГЕЙТ НА «ТРИ ЗАКОНА АРХИТЕКТУРЫ» (обязателен перед планом)

ЛЮБАЯ доработка проходит три фильтра (AGENT.md / README.md «Философия дизайна»):
1. **только универсальные механики** (движок без жанрового «закона»);
2. **общие базовые возможности** — отображение/справки/проверка;
3. **код лишь проверяет возможность, решает мастер-рассказчик** (тест: «Код решает за мастера?» — если да, переделай в «проверку»).

Инфраструктурные и UX-пункты (бэкапы, метрики, git, адаптив, WebSocket) к законам **нейтральны**.
Игровые идеи из «Актуального анализа», которые гейт **не прошли**, вынесены в конец файла в
раздел «❌ ОТКЛОНЕНО» — чтобы не пережёвывать их каждую сессию.

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ (сессия 38 + хвосты, закрытые в сессии 39)

| Область | Статус |
|---------|--------|
| **Git-репозиторий** `github.com/pL1uXa-AI/text_game_rpg` (приватный): `.gitignore` (секреты/`data/`/логи/папка идей), `.gitattributes` (LF + CRLF для .bat), `.env.example`, `requirements.txt` + `requirements-optional.txt`, `scripts/setup_env.bat` — окружение с нуля | ✅ сессия 33 |
| **Безопасность**: сервер слушает **только 127.0.0.1** (был `--host 0.0.0.0` при живых ключах и отсутствии авторизации); `.env.*` (включая `.env.bak` с ключами) исключены из git; ключи не уходят ни в `/api/*`, ни в JSON-дамп | ✅ сессия 33 |
| **Переносимость мира**: `GET /api/worlds/{id}/export/json` (состояние + все события + карточки + лор + слоты + граф) и `POST /api/worlds/import/json` (создаёт НОВЫЙ мир, память перестраивается фоново); кнопки «🗄 Полный дамп» / «📥 Импорт из дампа» в UI; авто-бэкап `data/game.db` при старте (`data/backups/`, SQLite Backup API, ротация `BACKUP_KEEP`) | ✅ сессия 33 |
| **Метрики переживают перезапуск**: `data/metrics.jsonl` + `restored_from_journal` в `/api/metrics` (раньше буфер был чисто in-memory и обнулялся рестартом) | ✅ сессия 33 |
| **Авто-детект окна модели** (`llm.probe_context` + `routers/core.py::_context_guard`): llama.cpp `/props`, Ollama `/api/show`, OpenAI-совместимые `context_length` из `/models`; мир не может заявить контекст больше реального n_ctx (защита от молчаливых обрезов), UI показывает потолок | ✅ сессия 33 |
| **Фикс админки**: «Сбросить всё к .env» больше не выставляет тумблеры в `false` (раньше после «сброса» молча выключались судья логики, автономный мастер, боевой ИИ, события и озвучка) — сброс идёт явным списком `reset` | ✅ сессия 33 |
| **Тесты слоя памяти и озвучки** (`tests/test_memory_layer.py`, 41 тест): BM25/гибрид, порог косинуса (cloud vs local), коллекции по размерности, `retrieve_memory` (отбор/K/деградация/логирование отказа), индексация (id-схемы, `world_id`, обрезка простыни), сводки (fold только при успешной сводке), чанкинг лора, подготовка текста/кэш-хеши/`_norm_rate` в TTS. Раньше весь слой был заглушён в conftest и не покрывался НИЧЕМ | ✅ сессия 33 |
| **Три закона**: движок директив (Цепочка обязанностей), код не решает за мастера | ✅ |
| Мега-пакет механик (сессия 32): таймеры, экипировка/слоты, потребности/рассудок, календарь/сезоны, локации-зоны, доска объявлений, звания во фракциях, сны/видения, ставки на бросок | ✅ |
| Фоновые агенты: судья логики, автономный «мастер», боевой ИИ врагов, динамические события, Провидение | ✅ |
| Память 5+1 слоёв (краткосрочная, сводки, RAG, карточки сущностей, карточки знаний, лор-библия) + динамическая выдача от размера контекста | ✅ |
| Сюжеты-миры файлами в `plots/` (3 комплектных: «Расколотые Небеса», «Теневой Торговец», «Пепел Контракта»), 8 рассказчиков, 33 жанровые подсказки, свой сюжет | ✅ |
| Графовая БД карты (сессия 27), экономика/вес/станции/крафт (28), фракции→геймплей (23), динамическая сложность (24) | ✅ |
| Озвучка (Edge/Piper/Kokoro), rate-limit, профилирование, SSE-стриминг, режим мастера, слоты + автосохранение, экспорт текста | ✅ |
| **Тесты (pytest)** | ✅ **388 тестов**: схемы директив (золотой корпус), механика (+с32), API (44 эндпоинта), граф, админка, интеграция (10 ходов), память/озвучка (с33), переносимость (с33), авто-детект контекста (с33), перемотка/фичи (с34), баг-хант (с35), аудит-фиксы (с36), ruff-чистка+фикс C5 (с37). `python -X utf8 -m pytest` |

---

## ✅ DONE (реализовано — подробности в AGENT.md)

### Спринт 34 — реальность и устойчивость: логи, бюджеты контекста, перемотка, очередь фоновых агентов, SSE, UX-фичи
> Пакет по выбору пользователя: A1–A4 (баги памяти/сохранений), B1–B5 (скорость/устойчивость),
> C1,C2,C4–C9 (игровой опыт, кроме C3-мультиголоса), D3 (SSE), E1,E3 (логи/метрики), E2 (линтинг).
> Подробности и подводные камни — в AGENT.md «Сессия 34».
- **A1/A4/C1**: перемотка к ходу (rewind_to delete/hide), fold 0/1/2, turn_snapshots,
  `POST /rewind`, `GET /rewind/points`; load_save переписан (была потеря недавней истории).
- **A2/B4**: бюджет recent от ИЗМЕРЕННОГО промпта + доли окна; build_messages гарантирует
  невыход за окно; ярусы промпта (−30% токенов в мире без подсистем; 9/9а/16 не трогаются).
- **B3**: `bg.py` — очередь фоновых LLM-агентов (приоритет, семафор, пауза на ход игрока),
  все 6 агентов переведены; статистика в `/api/metrics`.
- **B2**: ретраи с бэк-оффом для LLM/Chroma/облака (retry.py, is_transient).
- **B5/A3**: SQL-страницы и ограниченные выборки (get_unfolded_events/count_events/history),
  индексы; количество сводок масштабируется (было жёсткое [-3:]).
- **D3**: шина событий мира (bus.py) + `GET /events/stream` (SSE); поллинг — запасной.
- **C2/C5/C6/C7/C8/C9**: дневник (вкладка + /journal note), /risk (9 осей ресурсов),
  ружья Чехова (на горизонте), тайны NPC (notes), статусы/метки врагов (не тикают),
  итог квеста + дедлайн (quest_success/fail + timer). Правила 33–36 в промпте.
- **E1/E3**: JSON-лог + контекст хода; метрики обрезов (cut_by_limit/cut_mid), оценки
  релевантности RAG; оставшиеся тихие except → лог.
- **E2**: ruff.toml/mypy.ini + CI-шаги (ruff обязателен, mypy/coverage информационные);
  .env.example + 15 ключей (LOG_*, LLM_RETRIES, LLM_BG_*, PROMPT_TIERS, TURN_SNAPSHOT_KEEP).
- **Тесты**: 303 passed (было 278). Сервер перезапущен на 8002, живые smoke подтвердили
  journal/risk/rewind/metrics/log-контекст.

### Спринт 33 — git, безопасность, переносимость и закрытие «реальных проблем» из анализа
> Основание: «Актуальный анализ.txt» оказался частично устаревшим (не знал о сессиях 24–32:
> боевой ИИ, автономный мастер, динамическая сложность, графовая БД, мега-пакет механик;
> «только один сюжет» — их три; «>180 тестов» — 206; «декомпозицию narrator.py можно сделать» —
> сделана в сессии 30). Поэтому план строился по итогам **проверки кода**, а не по тексту анализа.
- **Git**: `git init` (main) + `origin` на приватный `pL1uXa-AI/text_game_rpg`. `.gitignore` переписан:
  секретные `.env` и `.env.*`, вся папка `data/` (841 МБ: game.db, chroma/, chroma-venv/, tts/, fastembed/),
  логи, `nul`, `__pycache__`/.pytest_cache, черновик `scripts/_test_repair.py`, локальная папка заметок
  `НЕ УДАЛЯТЬ ПАПКУ. ИДЕИ/`. `.gitattributes`: LF в репозитории, CRLF для `.bat` (cmd.exe), binary для моделей.
  В индекс — только код/доки/`plots/`/`.env.example` (72 файла).
- **Секреты**: `.env.example` (53 параметра, `*_API_KEY=` пустые) вместо отсутствующего шаблона;
  правило 15 в AGENT.md; проверка перед пушем `git diff --cached | grep -ciE "sk-[A-Za-z0-9]{12,}"` → 0.
  В рабочем дереве остался `.env.bak` с ключами — игнорируется и **не** удалён (решение о удалении за владельцем).
- **Воспроизводимость**: `requirements.txt` (fastapi 0.141.1 / uvicorn 0.50.2 / pydantic 2.13.4 /
  httpx 0.28.1 / aiosqlite 0.22.1 / pytest 9.1.1 — зафиксированы по рабочему окружению) +
  `requirements-optional.txt` (edge-tts, sherpa-onnx+onnxruntime, fastembed — все импорты в коде ленивые,
  без них игра работает) + `scripts/setup_env.bat` (.env из примера → pip install → свой venv ChromaDB →
  `data/chroma`). `rank-bm25` не нужен — BM25 свой, в `embeddings.py`.
- **Смена хоста**: `start_game.bat` → `--host %GAME_BIND%` (по умолчанию `127.0.0.1`); вывод с
  пояснением, как сознательно открыть наружу.
- **Переносимость**: `db.world_dump()` / `db.restore_world()` / `db.backup_database()`;
  эндпоинты `GET /api/worlds/{id}/export/json[?download=1]` и `POST /api/worlds/import/json`
  (схема `ImportIn`); фоновая перестройка памяти импортированного мира
  (`routers/worlds.py::_reindex_imported_world`: лор → карточки → обмены парами «действие → ответ»).
  Дамп **обязан** быть без ключей (`api_key` вырезается из `provider_settings`); импорт всегда заводит
  новый мир (не затирает); стартовый бэкап БД с ротацией (`BACKUP_DB_ON_START`, `BACKUP_KEEP`).
- **Метрики на диске**: `metrics._persist()`/`read_journal()` (jsonl, повреждённые строки пропускаются),
  `as_json()` достраивает окно из журнала при пустом буфере; флаг `METRICS_PERSIST/METRICS_FILE/METRICS_TAIL`.
- **Авто-детект контекста**: `llm.probe_context()` (3 источника, кэш по (base_url, model), тихий фолбэк None)
  + `core._context_guard()` (снижает `context_tokens` до 95% лимита, пишет `context_limit`/
  `context_limit_source` в `gen_settings`; при неизвестном лимите НЕ додумывает); вызывается при создании
  мира и сохранении настроек; UI показывает потолок и предупреждает до «применить». `DETECT_MODEL_CONTEXT`.
- **Правило 14 (не молчать)**: `memory.retrieve_memory` глотал ЛЮБУЮ ошибку без лога (отказ Chroma был
  неотличим от «эмбеддинги выключены») → теперь `log.warning` с world_id; легальный фолбэк выключенных
  эмбеддингов остался тихим и проверяется тестом.
- **Тесты**: `tests/test_world_dump.py` (11), `tests/test_memory_layer.py` (41), `tests/test_session33.py` (21)
  + фикстура `fake_config` в conftest (подмена кэшированного Config без порчи реального) и отключение
  персиста/бэкапа/детекта в тестах (иначе pytest писал бы в боевой `data/`). Итог **278 passed**,
  `scripts/test_directives.py` 4/4, `check_plot.py` 3/3, `node --check` app.js + скрипт админки,
  сервер перезапущен на 127.0.0.1:8002, живой smoke: создание мира (limit 1 048 576 от облака — 262k оставлен),
  ход, дамп (ключей нет), импорт → новый мир (4 события/18 карточек/23 лор-статьи) → cleanup,
  `data/backups/game-*.db`, `data/metrics.jsonl`, `/openapi.json` 44 пути.
- **Документы**: README (быстрый старт через setup_env.bat, предупреждение про 127.0.0.1, 8 рассказчиков
  вместо «7», актуальное дерево `backend/` с `routers/`, `graph.py`, `directives.py`, `schemas.py`,
  `ratelimit.py`; таблица API + дамп/импорт; новые `.env`-ключи; совет про бэкап), AGENT (правила 3/5/14
  уточнены + новое правило 15 «Git и секреты», раздел «Как тестировать» → шаг 0 + проверки дампа/бэкапа,
  8 рассказчиков).

### ✅ Сводка сессии 39 (закрытие хвостов 38)
Семь хвостов закрыты, один — частично (остаток D11), два ждут владельца (B1/B3, свой сюжет в
`plots/user/`). Итоговые проверки: **523 теста** (было 495) — +`tests/test_session38_tails.py`
(27: E1/E6/E7/D5/D11/D15/B1, включая degradation-пробы «сломанный вход → поймано»),
`ruff check backend scripts tests` — 0, `mypy -p backend` — **0 замечаний при
check_untyped_defs = true**, `check_frontend.py` / `check_plot.py` / `check_start_bat.py` —
зелёные, `scripts/test_directives.py` 4/4, сервер перезапущен на 127.0.0.1:8002 (живой smoke:
`/api/themes` 200, `/api/plots/errors` пуст). Правки: `frontend/app.js` (делегат + 15 мест +
переключатель панели), `frontend/style.css` (4 `@media`), `frontend/index.html`
(`#btn-sidebar`, `#sidebar-scrim`), `backend/db.py` (помощники `_one_row/_as_dict/_rowid`),
по месту — `narrator/mechanics/memory/journal/metrics/embeddings/chroma_client/plots/routers`,
`mypy.ini` (строгость включена), `scripts/check_frontend.py` (`dataclick_check`, `dynamic_check`,
`mobile_check`), `scripts/check_plot.py` (`PLOT_SCHEMA`), новый `scripts/check_start_bat.py`,
`scripts/setup_tts.bat` (D13), `.github/workflows/ci.yml` (шаг bat-проверок).
**Три закона не затронуты**: правки — UI, типы и инструменты проверки; код по-прежнему только
проверяет возможности (в `mechanics.py` изменён лишь способ прочитать `turns` эффекта — тот же
результат: нечисловое значение → постоянный эффект). Тест `test_three_laws_identical_in_docs`
зелёный.

---

## 🟡 NEXT — ближайшие (высокий эффект / низкая цена)

### 🔴 Хвосты сессии 38 → закрыты в сессии 39 (остались два, оба ждут ВЛАДЕЛЬЦА)
Аудит 38 нашёл 58 пунктов. Восемь хвостов из списка ниже разобраны в сессии 39
(проверки — `tests/test_session38_tails.py`, 27 тестов; всё по-прежнему в зелёном pytest,
`ruff`, `mypy -p backend` и трёх чекерах `scripts/check_*.py`):

- [x] **[E1] `onclick`-литералы → data-атрибуты + один делегат** — все 15 мест переведены на
      `data-click`/`data-arg`, обработчик — один делегат на `document` (реестр `CLICK_ACTIONS`,
      тип аргумента задаёт `CLICK_NUMERIC`: ключи словарей сущностей всегда строки, индексы —
      числа). `jsAttr()` удалена из `app.js` вместе с классом риска: в атрибуте теперь ДАННЫЕ,
      а не исполняемый JS, экранирование — обычное `esc()` (обёртка `attrArg()`).
      Чекер `dataclick_check` запрещает возврат к inline-`onclick`/`javascript:` и требует,
      чтобы каждый `data-click` был в реестре, а каждый `data-arg` — через `attrArg/numAttr`;
      roundtrip грязных ключей (`don't`, кавычка, `';x=1;//`) проверяется на node.
      *(законы нейтральны: только UI)*
- [x] **[E7] Явный `DYNAMIC_IDS`** — whitelist стал словарём `маска → обоснование`, и чекер
      проверяет его в обе стороны (`dynamic_check`): маска обязана реально собираться в `app.js`
      (иначе она врёт и опечатка в `$("pl-lore")` снова пройдёт), а каждый id, назначенный в
      `app.js` и отсутствующий в разметке, обязан попасть под маску — иначе это мёртвый id.
- [x] **[E6] Мобильная адаптивность (минимум по пункту)** — `@media` было 0, стало 4
      (≤900 / ≤560 / ≤420 + `prefers-reduced-motion`): панель мира на узком экране —
      выдвижная (`#btn-sidebar` + затемнение `#sidebar-scrim`, закрытие по Esc и по фону),
      тап-цели ≥44px, шрифт чата/поля 16px (iOS не зумит страницу), карта 62vh. Кольца карты
      на 375px правки не потребовали: узлы рисуются в `viewBox` и подгоняются `fitAll`.
      Полноценная адаптивнная сессия (жесты, PWA, реальный просмотр на телефоне) — по-прежнему
      в 🟡 NEXT ниже, но хвост «0 @media» закрыт и закрыт проверкой (`mobile_check` +
      degradation-пробы в pytest).
- [x] **[D11] структурная часть** — проверки `.bat` переехали в `scripts/check_start_bat.py`
      (шаг CI) и ловят: зашитый абсолютный путь python (D13), отсутствие `-V`-проверки и
      значения по умолчанию, жёсткий `0.0.0.0` (правило 5), возврат к `errorlevel` вместо
      `%{http_code}`, `javascript:`-ссылки. В `tests/test_session36_bugfixes.py` рядом с
      оставшимися smoke-по-тексту тестами прямо написано, что регрессий они не дают.
      **Остаток D11 открыт:** живой `cmd /c`-харнесс с подменённым портом и node-харнесс для
      `app.js` — отдельная сессия (исполнять bat в CI нельзя: он поднимает сервер и жмёт pause).
- [x] **[D5] Остатки mypy** — все 35 замечаний разобраны по месту, `check_untyped_defs` ВКЛЮЧЁН
      (`mypy -p backend` — чисто, 38 файлов). Массового `# type: ignore` нет: осталось 3
      осознанных места (кэш Config, `run_coroutine_threadsafe`, Union в `graph.py`), их число
      сторожит тест. Приёмы: помощники `db._one_row/_as_dict/_rowid` (вместо молчаливого
      `Row|None` → `int(None)` — внятный `LookupError`, правило 14), честный тип у
      `use_tools_for_provider`, `_safe_int` вместо `int()` в try/except, приведение `str(...)`
      в показаниях состояния, `float(...)` в агрегатах метрик, пустой вектор эмбеддинга теперь
      ошибка, а не «RAG ничего не нашёл».
- [x] **[D13-хвост] `scripts/setup_tts.bat`** — приведён к тому же приёму, что `setup_env.bat`:
      `if "%PY%"=="" set "PY=python"` + проверка `"%PY%" -V`, абсолютный путь убран (его ловит
      `check_start_bat.py`).
- [x] **[D15-хвост] Строгая структура сюжетов** — в `check_plot.py` добавлена декларативная
      `PLOT_SCHEMA` + рекурсивный `_tcheck` (без внешних библиотек: `jsonschema` в requirements
      тянул бы завись ради одного валидатора). Проверяются типы и обязательные поля метаданных,
      лора, фракций, стартового состояния, инвентаря/магазинов, актов/глав и цепочек квестов;
      «свои» поля автора не запрещены (закон 1). Валидный JSON с битой структурой больше не
      проходит — и не исчезает молча: попадает в `plots.SCAN_ERRORS` → плашку в окне «Новый мир»
      (живая ветка «битый файл в `plots/user` → плашка» проверена тестом с настоящим файлом,
      закрывает и пункт [plots] ниже). Проверки деградации: сломать `name`/`gold`/`status`/
      `logline` — каждая проба обязана быть поймана.

Остаются открытыми только те, где решение НЕ за кодом:
- [ ] **[B1/B3] Удаление файлов рабочей копии — ждёт решения владельца** (я удалять без спроса
      не стал): `.env.bak`, `.env.bak.session36` (в каждом 3 живых ключа; в git никогда не
      были — `.gitignore` кроет `.env.*`), `nul`, корневые `server*.log`, `data/server*.log`,
      старый `data/chroma.log` (5.3 МБ), `data/fastembed_test/` (17 МБ, копия модели),
      `scripts/_test_repair.py`. Что уже сделано вместо удаления: все они под `.gitignore`,
      журнал Chroma перенесён в `data/logs/`, а тест
      `test_b1_trash_files_stay_out_of_git` не даст им просочиться в индекс. При подозрении на
      компрометацию ключей — ротация в RouterAI (решение владельца, не кода).
- [ ] **[plots] Свой сюжет игрока в репозитории** — механика «битый файл → плашка» покрыта
      тестом, но живого пользовательского сюжета в `plots/user/` по-прежнему нет: на первом
      реальном файле стоит поглядеть плашку глазами (CI-шаг прогоняет `check_plot.py` и по
      `plots/user`, включая новую строгую схему).

### Открытые реальные проблемы (из проверки кода, не из текста анализа)
- [ ] **[AN-факт] `data/` целиком в `.gitignore` → резервных копий нет вне машины** (кроме дампа/бэкапов
      в той же папке). Добавить «выгрузить всё» (скачать `data/backups/*.db` + дампы миров) и пометку в README.
- [ ] **[PR] Мультиголос NPC (TTS)** — `voice` в `npc_set`/`companion_add`, синтез сегмента голосом
      говорящего. Голос — оболочка, сюжетно нейтрально. *(эффект выс/сложность сред)*

> ✅ **Закрыто в сессиях 34–38** (было в этом списке как «не сделано»): структурированное
> логирование (`logsetup.py`, JSON-лог + контекст хода + `request_id`, тихих `except: pass` — 0),
> дневник приключений (`journal.py`, `/journal`, вкладка), «заметки мастера» NPC (`npc_set.notes`,
> правило 35), `/risk` (`risk.py`, слэш-команда + REST), ружья Чехова (работают с сессии 38 —
> A1: запись состояния перенесена после всех мутаций хода).

### Из «Актуального анализа» — прошло гейд и реально новое
- [ ] **[AN] `quest_fail` / `quest_success`** — итог квеста как отдельная ступень: код лишь записывает
      статус и следствие (`quest_done {id, next}` уже есть), чем закончился провал, решает мастер.
- [ ] **[AN] Таймер, привязанный к квесту** — `quest.timer` → `setting.timers` (механика с32 уже тикает);
      по истечении движок пишет «истёк», провал/успех ведёт мастер.
- [ ] **[AN] `area_effect` / `targets: []`** в `ability_use`/`effect_add` — хранение списка целей и показ;
      урон по-прежнему только директивой мастера (иначе закон 3).
- [ ] **[AN] Эффекты на NPC/врагов** (`enemy_effect_add/remove`) — **только хранение**, без авто-тика
      (урон/лечение ведёт мастер; согласовано с пометкой ⚠️ в старых планах).
- [ ] **[AN] Инициатива/позиции как ПОДСКАЗКА** — `player.initiative`/флаг позиции показывать в
      `format_state` (+ мод от ловкости задаёт мастер в `roll.mod`). Боевой планировщик в коде — нельзя.
- [ ] **[AN] Ритуалы с компонентами** — крафт-рецепт с требованием `time`/`weather`/`station`/`location`;
      проверка возможности уже есть в `CraftHandler`, нужны лишь поля и отказ с объяснением.
- [ ] **[AN] Объекты окружения с состоянием** — `setting.objects` + флаги (дверь/рычаг/сундук), показ в
      состоянии, изменения только директивами. Частично покрыто флагами и локациями-зонами.
- [ ] **[AN] Улучшение инструментарию авторов**: ещё 3–4 стандартных сюжета разных жанров
      (киберпанк/детектив/космос/хоррор) — единственный способ **доказать закон 1** на практике;
      сейчас их 3 (тёмное фэнтези+литрпг, эконом-триллер+киберпанк, литрпг).

---

## 🟢 PLANNED — среднесрочные (глубина механик и мира)

- [ ] **[PR] «Чеховские ружья» — проспективные намёки** [PR №3]: не ждать застревания (мастер с25), а
      сеять поворот заранее: пометка в `format_state` («на горизонте …») + правило в промпте.
      Закон 2/3: это подсказка-отображение, «выстрелит» ружьё или нет — решает мастер.
- [ ] **[AN] Социальные механики как справочник, не как сценарий**: «диалоговые деревья» в коде
      против free-form рассказчика — допустимо лишь как `npc_set.topics`/заметки (что NPC знает и
      как реагирует по ступени репутации — правило 26 уже работает). Слухи = `board_add` + карточки.
- [ ] **[AN] Спутники: личные квесты и события лояльности** — квест `quest {companion}`, показ в
      состоянии; рост уровня/верности — только директивами мастера (`companion_update`), не авто-XP.
- [ ] **[AN] «Дома и база» [ПР/PR]** — жильё = локация с флагом + `station`, отдельный склад
      (`home_storage` как вместимость вне рюкзака), безопасность = флаги/эффекты/охрана-NPC.
      ⚠️ закон 1: никаких жанровых «особняков», только логистика.
- [ ] **[AN] Динамика мира по флагам** — «город разрушен» → мастер правит карту директивами;
      код может лишь подсвечивать связи (локация ↔ фракция ↔ квест) в `format_state`/графовой БД.
- [ ] **[PR] Квесты, открывающиеся по репутации/званию** — код **показывает** ступень/звание и
      **отказывает** в проверке возможности, решение заводить квест — за мастером (правило 26).
- [ ] **[AN] Ремесленные специализации** — `profession {name, buff}` + теги станции: «кузнец-оружейник»
      реализуется творческой профессией и `station`, новых жанровых полей в движок не нужно.
- [ ] **[AN] Школы/синергии способностей** — `ability.school` есть; допустимо как справочник-подсказка
      («огонь + ветер»), эффект считает мастер директивами.
- [ ] **[PR] Декомпозиция `narrator.py` дальше** (`build_system_prompt`, `format_state`) — [ПР] частично:
      память/лор/персонаж вынесены (с30), правила можно переложить в `narrator_data.py`.
- [ ] **[AN] Импорт/экспорт истории в markdown/PDF с картой** — текст-экспорт и JSON-дамп есть;
      md/PDF — презентационный слой (закон 2).

---

## 🔵 BACKLOG — на будущее (технологии, мультиплеер, фичи)

### Оффлайн / локальность
- [x] Переключение провайдеров LLM (llama.cpp / Ollama / OpenAI-совместимые) — ✅ с3
- [x] Локальные эмбеддинги FastEmbed (оффлайн, коллекция по размерности) — ✅ с11–12
- [x] **Локальный `n_ctx` больше не ловушка** — авто-детект окна модели (✅ с33)
- [ ] **Docker-сборка**: chroma + api + frontend одной командой (щад `setup_env.bat` уже снимает боль)

### Мультиплеер (⚠️ закон 3 — только через движок директив)
- [ ] Общие миры через WebSocket-комнаты, синхронизация состояния.
- [ ] Спектаторы/стриминг.
- [ ] Второй агент-игрок (NPC с ИИ): действует **только** директивами того же движка, мастер — арбитр.

### ИИ-качество
- [x] Метрики качества ответов + трассинг агентов (✅ с22.6/с30), журнал на диске (✅ с33)
- [x] Автономный «мастер» (✅ с25), боевой ИИ врагов (✅ с26), сны/видения (✅ с32)
- [ ] **Качество длинных ответов**: метрика «обрыв фразы» как отдельный сигнал + порог алерта в UI
      (сейчас дедуп и дописывание есть, а статистики по частоте обрезов нет).
- [ ] **Оценка релевантности RAG** (попадает ли вспомненное в тему хода) — пока видна только «🧠 Память (N)».

### Технические
- [x] pytest-набор (✅ с11), интеграционные тесты (✅ с30), **слой памяти/озвучки (✅ с33)**
- [x] Rate limiting, LRU-кэш эмбеддингов, async-Chroma (httpx), графовая БД, профилирование
- [x] **CI**: GitHub Actions `.github/workflows/ci.yml` — pytest + `test_directives.py` +
      `check_plot.py` + `check_frontend.py` (node --check и сверка id-ссылок) + импорт backend +
      скан секретов (✅ сессия 33)
- [ ] **Структурное логирование** (structlog/JSON), трейсинг ходов, метрики стоимости токенов
- [ ] **WebSocket вместо SSE-поллинга** (нужны сервер+клиент+auth; SSE работает — низкий приоритет)
- [ ] Кеш результатов эмбеддингов на диске (LRU in-memory уже есть)
- [ ] Переносимость `requirements*.txt` на другие ОС/python в PATH (щас жёсткий путь в `start_game.bat`)
- [x] `start_game.bat` устойчив к «порт занят» (✅), + жёсткий bind на 127.0.0.1 (✅ с33)

### UX
- [ ] Голосовой ввод (Web Speech API / локальный whisper).
- [~] Мобильная адаптация: минимум по хвосту E6 сделан в с39 (4 `@media`, выдвижная панель мира,
      тап-цели 44px) — **остались PWA, жесты и просмотр на реальном телефоне**.
- [ ] Саундтрек/эмбиент по жанру мира.
- [ ] Интерфейс на 2+ языках (полный перевод UI).
- [ ] **Визуальный редактор сюжетов** [AN] — снижает порог входа для авторов (сейчас JSON по PLOTS.md
      + валидатор `check_plot.py`).
- [ ] Компас «соседние локации» под ходом [PR] (часть есть на карте и в `/where`).

### Из ТЗ, что не сделано
- [ ] Генерация подсказок при застревании — есть `/hint` + автономный мастер (✅ с25); сделать `/hint` авто.
- [ ] Смена перспективы на лету (сейчас только при создании мира).

---

## 📜 ЛОГ СПРИНТОВ 4–32 (сжато; полные детали — в AGENT.md по номерам сессий)

- **с4** статус-эффекты, статы/класс/профессия/навыки/титулы/репутация, читаемые флаги в UI, фикс блока жанров.
- **с5** полная RPG-система (7 статов + производные, 8 рас / 6 классов / 9 профессий + творческие, ранги F..G, навыки, эффекты со стаками/модами), откат по снапшоту при перегенерации, карточки знаний, фикс per-world RAG (embed_query/embed_documents принимали provider), профессии от действий, скрытые сущности.
- **с6** нормализация кривых директив (`normalize_directives`, «str has no attribute get»), регрессия `test_directives.py`.
- **с7 / 7.1** SVG-карта локаций (BFS-кольца, клик = идти), динамические события по ходам, ИИ-быстрые действия.
- **с8** озвучка TTS: Piper (sherpa-onnx, ru-голоса) / Kokoro / Edge, фоновый синтез, кэш, per-world + админка.
- **с9** UX-фиксы: сохранение быстрых действий, скролл лога, потолок шанса события 0.5, Edge rate-нормализация, очистка текста для синтеза.
- **с10** надёжность: `db.transaction()`, логирование RAG/фона, лимит длины действия, фикс числовых id.
- **с11** рефакторинг: `app.py` 1349 → ~40 строк + `backend/routers/`, Pydantic-схемы (API + директивы с золотым корпусом), `admin_settings.py`, полный pytest (84).
- **с12–13** локальные эмбеддинги (FastEmbed, коллекция по размерности); стандарт `MAX_TOKENS=2000` / `CONTEXT_TOKENS=32768`, TTS = edge+Dmitry.
- **с14** «Ключи Пангеи» по книге, `theme.start_location` (локация ≠ название мира), персонаж-биография всегда, opening видит лор.
- **с15** богатый старт персонажа (`generate_character`: роль/уровень/статы/навыки/титулы/инвентарь), блок «🧙 Персонаж», кликабельные предметы.
- **с16** слэш-команды `/where /status /quests /map /inventory /story`, автосохранение (`upsert_auto_save`) + «⏱ Восстановить».
- **с17** пагинация лога (`before/limit`) + список миров/сюжетов странично.
- **с18** OpenAPI-теги/`/docs`, `BACKGROUND_TASKS_ENABLED`; время/погода → геймплей (`environment_mods`) и `npc_set.schedule`.
- **с19–20** ROADMAP-ревизия; A1 логирование вместо `pass`, A2 устойчивость судьи (t 0.2→0.05), A3 заметка о деградации памяти, C1 вынос `narrator_data.py`; авто-мена профессий отключена (решает мастер).
- **с21 (B2–B5)** интерактив карты (путь/фильтры), ветвление и цепочки квестов, сверхспособности, статистика/достижения.
- **с22** магазины/торговля/крафт/компаньоны; **с22.5** 👁 Провидение; **с22.6** `metrics.py` + метрика повторов.
- **с23** фракции → геймплей: ступени доверия, связи (союз/враг), разлив репутации, правило 26.
- **с24** динамическая сложность (`dynamic_adversary_scale`, частота событий от уровня).
- **с25** автономный «мастер» (`master_stuck_reason` + нудж); **с26** боевой ИИ врагов (без двойного урона).
- **с27 / 27б** графовая БД (узлы/рёбра/BFS/путь, синк в транзакции хода), карта и `/map` на графе, типы узлов faction/npc/shop.
- **с28 / 28б** вес предметов и вместимость, карманы NPC/врагов, станции и требование профессии для крафта, справки `/economy`.
- **с29 / 29.1** сюжеты-миры **файлами** в `plots/` (+`plots.py`, снапшот темы в мире), рассказчики файлами, `check_plot.py`, UI-мелочи состояния.
- **с30** декомпозиция `narrator.py` → `memory.py` / `lore_retriever.py` / `character_generator.py` (фасад с реэкспортом), интеграционные тесты, трассинг агентов, предзагрузка TTS, rate-limit, LRU-кэш эмбеддингов, DnD-удаление сохранений.
- **с31** память масштабируется от размера контекста (`dynamic_memory_k`/`dynamic_lore_budget`), все параметры в Админке/Настройках, защита от обрывов/дублей ответа, быстрые действия не сбрасываются.
- **с32** мега-пакет универсальных механик: ⏳ таймеры, ⚔️ экипировка/слоты, 🍖 потребности + 🧠 рассудок, 📅 календарь/сезоны, ⛰ локации-зоны, 📜 доска объявлений, 🏅 звания во фракциях, 🌙 сны/видения, 🎲 ставки на бросок (+ревизия найденных косяков).

---

## ❌ ОТКЛОНЕНО (идеи, не прошедшие гейт Трёх законов)

Зафиксировано, чтобы не возвращаться без переформулировки. Все — из раздела 4
«Актуального анализа»; там они предлагались «в лоб», без проверки.

| Идея [AN] | Почему отклонена в текущей формулировке |
|-----------|------------------------------------------|
| Динамические цены (спрос/предложение), аукционы, инвестиции в локации | Код начинает **сам считать** экономику и выдавать результат без решения мастера → закон 3. Допустимо: мастер задаёт цену/запас директивой, код проверяет золото/вес (есть с28) |
| Контрабанда как механика | Жанровое «зашитое» понятие → закон 1. Реализуемо сейчас: `flag` + репутация + `add_item` |
| Перки/таланты, которые **игрок выбирает при достижении уровня** | Автопрокачка по уровню — прямое нарушение закона 3 («прогресс — творчество рассказчика»). Возможно лишь как справочник + директива мастера |
| Авто-XP и авто-уровень спутников | То же: `companion_update {level, loyalty}` уже есть, счётчик роста в движке — нельзя |
| Автоматические сдвиги отношений между фракциями | Разлив по объявленным связям (`relations` союз/враг) есть с с23 и запускается **дельтой мастера**; самостоятельный движок «решит, кто с кем воюет» — закон 3 |
| «Перестройка карты по глобальным флагам» | Код менял бы мир без директив. Правильно: мастер делает `location_update`/`remove`, код показывает связи |
| Полноценная боевая система: очередь инициативы + зоны поражения как **движок боя** | Движок обслуживания, не смысл: остаётся подсказкой в состоянии (см. 🟡), бой ведёт рассказчик |
| Авто-назначение профессии/класса по счётчикам | Отключено сознательно (с19); счётчики дел остаются, решение за мастером |

---

## ⏭️ РЕКОМЕНДУЕМЫЙ СЛЕДУЮЩИЙ СПРИНТ (порядок)

1. **Адаптивность UI + мобильный чат/сайдбар** — единственная реальная «болячка» игрока.
   Хвост E6 закрыт в с39 (брейкпоинты, выдвижная панель, 44px, 16px): играть с телефона
   стало можно, но это ещё не «родной» мобильный опыт — нужны жесты, PWA и живой просмотр
   на устройстве; заметно без LLM и без риска для законов.
2. **Мультиголос TTS (C3)** — самое заметное улучшение «живости» из готового слоя озвучки
   (`voice` в npc_set уже хранится, остался синтез сегмента голосом говорящего).
3. **Фронтенд-полировка нового**: реальная браузерная проверка вкладок «Дневник», перемотки,
   компаса, SSE (smoke в браузере, не только curl); админка под новые ключи (секция «Наблюдаемость»).
4. **3–4 новых стандартных сюжета** (киберпанк/детектив/космос/хоррор) — доказательство универсальности
   движка (закон 1) + контент для новых игроков.
5. **`area_effect`/`targets` и эффекты на NPC** — дорасширить (хранение уже есть, закон 3 соблюдён);
   PWA/горячие клавиши — в 🔵 Backlog.

---

*Обновление: сессия 2 — создан; сессия 19/22.5 — актуализация планов; сессия 28+ — ревью всех планов
на Три закона (спорные игровые доработки помечены ⚠️ и переформулированы); **сессия 33 — слит
PROPOSALS.md (файл удалён), добавлены `[AN]`-идеи из «Актуального анализа» с гейтом и раздел
«❌ ОТКЛОНЕНО», закрыты реальные проблемы (git/секреты/зависимости/переносимость/метрики/n_ctx/
тесты памяти), пересобран «СЛЕДУЮЩИЙ СПРИНТ»*.
