# ── Черновики/прототипы вне основного кода ───────────────────────────
scripts/_test_repair.py

# ── Секреты и локальные конфигурации ─────────────────────────────────
# .env содержит реальные API-ключи (RouterAI / OpenAI-совместимый / реранкер).
# В репозитории только .env.example (без ключей).
.env
.env.*
!.env.example

# ── Данные игры (локальные, большие, приватные) ───────────────────────
# game.db — миры/события/карточки/лор/слоты; chroma* — векторная память;
# tts/fastembed* — модели и кэш озвучки и локальных эмбеддингов.
data/

# ── Логи и артефакты запуска ──────────────────────────────────────────
*.log
logs/
server*.log
nohup.out
# Windows-артефакт от `... > nul`
nul

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── IDE / редакторы / ОС ──────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
desktop.ini

# ── Локальные заметки/идеи (не код проекта) ───────────────────────────
# Папка с анализами и набросками — остаётся локально.
НЕ УДАЛЯТЬ ПАПКУ. ИДЕИ/

# PID локального запуска сервера (не часть проекта)
.server.pid
